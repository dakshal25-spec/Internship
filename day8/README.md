# Day 8: CSV Processing

This folder contains a Python script that reads employee data from a CSV file and generates a formatted summary report.

## How to run the program:
1. Open your terminal.
2. Navigate to the `day8-csv` folder.
3. Run the program using the command: `python csv_processor.py`
4. Ensure `employees.csv` is in the same folder before running.
